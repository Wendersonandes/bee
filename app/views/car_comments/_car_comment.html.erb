        <% if carrinho.user == car_comment.user %>
          <div class="w3-row-padding w3-section" style="padding-top:25px; border-top:1px solid #eeeeee">
            <div class="w3-col s2" style="width:12%">
              <%= image_tag car_comment.user.avatar.url(:medium),class: "w3-circle", style: 'height:50px;width:50px' %>
            </div>
              <div class="w3-col s8" style="background-color:#eeeeee; border-radius:10px;padding:10px">
                <%= car_comment.content %>
                <% if car_comment.user == current_user %>
                  <%= link_to carrinho_car_comment_path(carrinho, car_comment), method: :delete, data: { confirm: "Are you sure?" },style:"float:right;", remote: true do %>
                    <span class="glyphicon glyphicon-remove delete-comment" style="color:#aaaaaa"></span>
                  <% end %>
                <% end %>
            </div>
          </div>
        <% else %>
          <div class="w3-row-padding w3-section" style="padding-top:25px; border-top:1px solid #eeeeee">
            <div class="w3-col s2 w3-right" style="width:12%">
              <%= image_tag "logo.jpg",class: "w3-circle w3-right", style: 'height:50px;width:50px' %>
            </div>
              <div class="w3-col s8 w3-right" style="background-color:#eeeeee; border-radius:10px;padding:10px">
                <%= car_comment.content %>
                <% if car_comment.user == current_user %>
                  <%= link_to carrinho_car_comment_path(carrinho, car_comment), method: :delete, data: { confirm: "Are you sure?" },style:"float:right;", remote: true do %>
                    <span class="glyphicon glyphicon-remove delete-comment" style="color:#aaaaaa"></span>
                  <% end %>
                <% end %>
            </div>
          </div>
        <% end  %>
