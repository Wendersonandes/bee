<div class="nested-fields">
  <div class="field">
    <%= f.collection_select :name, Mat.order(:name), :name, :name, include_blank: true %>
  </div>
  <div class="field">
  <%= f.label "Cores" %><br />
  <%= f.collection_check_boxes :color_ids, Color.all, :id, :name do |b| %>
    <div class="collection-check-box">
      <%= b.check_box %>
      <%= b.label %>
    </div>
  <% end %>
</div>
  <%= link_to_remove_association "Remover Material", f %>
</div>